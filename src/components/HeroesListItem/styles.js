// @TODO NEEDS CLEAN UP
import {
  StyleSheet,
} from 'react-native';

import {
  TEXT_2,
} from '../../constants/styles';

export default StyleSheet.create({
  listElementTitle: {
    fontSize: 16,
    flex: 1,
    color: TEXT_2,
    alignItems: 'center',
  },
});
