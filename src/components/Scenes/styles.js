import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabBarStyle: {
    borderTopWidth: 0.5,
    borderColor: '#b7b7b7',
    backgroundColor: 'white',
    opacity: 1,
  },
});
