import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  mainView: {
    marginTop: 64,
    marginBottom: 50,
    flex: 1,
    // remove width and height to override fixed static size
    width: null,
    height: null,
  },
});
