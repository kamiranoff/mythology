// @TODO NEEDS CLEAN UP
import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 20,
    paddingVertical: 10,
    borderRadius: 4,
    borderWidth: 1,
    borderColor: '#666',
  },
  text: {
    textAlign: 'center',
    justifyContent: 'center',
  },
});
